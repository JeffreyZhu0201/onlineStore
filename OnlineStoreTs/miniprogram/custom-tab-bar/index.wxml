<van-tabbar 
  active="{{ active }}" 
  bind:change="onChange"
  safe-area-inset-bottom
  active-color="#4CAF50"
  inactive-color="#808080"
>
  <van-tabbar-item 
    wx:for="{{ list }}" 
    wx:key="text"
    icon="{{ item.icon }}"
  >
    {{ item.text }}
  </van-tabbar-item>
</van-tabbar> 