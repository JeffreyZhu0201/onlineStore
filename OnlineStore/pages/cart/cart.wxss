/**index.wxss**/
page {
  height: 100vh;
  display: flex;
  flex-direction: column;
}
.scrollarea {
  flex: 1;
  overflow-y: hidden;
}

.van-submit-bar{
  position: fixed;
  height: calc(300rpx + env(safe-area-inset-bottom) / 2) !important;
}


