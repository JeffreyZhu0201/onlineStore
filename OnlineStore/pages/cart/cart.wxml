<!--index.wxml-->

<view class="block">
	<t-navbar
	 t-class-placeholder="t-navbar-placeholder"
	 t-class-content="t-navbar-content"
	 class="indexNavBar"
	 title="购物车"
	 t-class-title="nav-title"
	/>
</view>

<scroll-view class="scrollarea" scroll-y>
	<view class="container">
		<text>I'm cart</text>
		<view>
			<cartGoodCard good="{{good}}" />
		</view>

	</view>

</scroll-view>

<van-submit-bar
 price="{{ 3050 }}"
 button-text="提交订单"
 bind:submit="onClickButton"
 tip="{{ true }}"
>
	<van-tag type="primary">标签</van-tag>
	<view slot="tip">您的收货地址不支持同城送,
		<text>修改地址</text>
	</view>
</van-submit-bar>

<view style="width: 100%;height: calc(350rpx + env(safe-area-inset-bottom) / 2);" />

